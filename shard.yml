name: common_mark
version: 0.3.0
description: libcmark bindings, the reference CommonMark C library

development_dependencies:
  crystal_lib:
    github: manastech/crystal_lib

  minitest:
    github: ysbaddaden/minitest.cr
    version: ">= 0.5.0"

scripts:
  postinstall: cd ext && make

crystal: 0.20.0

license: BSD-2-Clause
